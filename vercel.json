{
  "buildCommand": "vite build && node -e \"const fs=require('fs'); fs.cpSync('.output/public', 'dist', {recursive: true});\"",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
